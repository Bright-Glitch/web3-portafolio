/** @type {import('next').NextConfig} */

module.exports = {
    reactStrictMode: false,
  images: {
    domains: ["gateway.pinata.cloud"],
  },
  devIndicators: {
        buildActivity: false
    }
}
